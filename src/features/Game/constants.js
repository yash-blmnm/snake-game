const GAME_STATUS = {
  none: "none",
  live: "live",
  over: "over",
  pause: "pause",
};

const INTERVAL = {
  beginner: 200,
  intermediate: 150,
  advanced: 100,
};

export { GAME_STATUS, INTERVAL };
