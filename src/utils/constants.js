const ROW_SIZE = 18;
const COLUMN_SIZE = 20;
const GAME_STATUS = {
  none: "none",
  live: "live",
  over: "over",
};
const DIRECTION = {
  left: 37,
  right: 39,
  up: 38,
  down: 40,
};
const INTERVAL = {
  beginner: 200,
  intermediate: 150,
  advanced: 100,
};

export { ROW_SIZE, COLUMN_SIZE, GAME_STATUS, DIRECTION, INTERVAL };
